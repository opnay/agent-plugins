# advance-codex-dev v0.1.1 Change Spec

## 변경사항 요약

- disallowed temporary-folder guidance를 제거하고 session artifacts의 기본 위치를 `.agents/sessions/{YYYYMMDD}/`로 정렬한다.
- `.agents/sessions` 폴더의 목적과 `turn-gate` plan/flow record 골격은 호출 가능한 skill이 아니라 docs-only convention으로 유지한다.
- `session-manager` skill, controller, tests, record schema, metadata를 제거하고 `turn-gate`를 active session flow의 소유 표면으로 둔다.
- `workflow-kit` autopilot state guidance도 같은 date-scoped session folder convention으로 맞춘다.
- subagent runtime skill 표면을 제거해 `advance-codex`에서 subagent orchestration guidance를 제공하지 않는다.
- manifest prompt, README, plugin spec이 plugin 사용 기준을 소유하게 한다.
- `skill-scenario-testing`을 수정 루프가 아니라 분석 중심 empirical evaluation skill로 정리하고, 기본 8개 scenario set, N개 단위 batch dispatch, subagent 한도 시 executor close-and-continue를 추가한다.
- `skill-creator`에 passive skill `description` 끝 plain token 목록 규칙을 추가한다.
- `pro-engineering` skill을 추가해 코드 작성과 문제 해결에서 엔지니어링 판단, 원인 분석, 구현 규율, 검증과 리스크 보고 기준을 제공한다.
- `pro-engineering` skill spec을 folder-based 구조로 전환해 사용자 의도, index, 세부 문제 해결 계약을 분리한다.
- `pro-engineering`에 직접 코딩 전에는 넓게 탐색하고 구현 단계에서는 좁게 집중하는 scope-control 계약을 추가한다.
- `pro-engineering`에 소유권 경계 계약을 추가해 새 동작, 상태, 검증, 대체 동작이 책임 위치를 옮기거나, 드러내거나, 중복시키거나, 흐릴 수 있을 때 소유 위치를 확인하도록 한다.
- `pro-engineering`에 허용 조건 우선 계약을 추가해 blocklist 조건을 늘리기 전에 허용되는 입력, 상태, 효과, 실패 조건을 먼저 정의하고, 차단 조건은 좁은 보조 방어선으로 두도록 한다.
- `pro-engineering`에 early-return 선호 기준을 추가해 guard, 실패 조건, 처리 불가 상태를 먼저 분리하고 정상 흐름을 선명하게 만들도록 한다.
- `pro-designer` skill을 추가해 UI, UX, Content, Quality core 기준과 별도 Color, Tone & Expression, Branding, Space & Composition, Surface & Form 축의 디자인 지식 기준을 제공한다.
- `pro-designer` Color 축을 추가해 color system, color mood, theme adaptability를 판단하게 한다.
- `pro-designer` Tone & Expression 축을 추가해 tone type, tone fit, tone construction, tone boundaries를 판단하게 한다.
- `pro-designer` Branding 축을 추가해 brand identity, brand assets, brand color role, brand consistency, functional conflict를 판단하게 한다.
- `pro-designer` Space & Composition 축을 추가해 spatial model, zone architecture, density and rhythm, information density fit, spatial hierarchy, flow and wayfinding, relationship geometry, responsive recomposition을 판단하게 한다.
- `pro-designer` Surface & Form 축을 추가해 surface model, depth, shape language, materiality, tactility를 판단하게 한다.
- `pro-designer`의 기존 기준을 조건문이나 실패 방지 문장 대신 판단 대상과 판단 목적이 드러나는 기준 문장으로 재정리한다.
- `pro-designer` UI, UX, Color, Surface & Form, Quality 기준을 폰트 크기, 간격, 색, 표면 필요성, 아이콘 의미, 상세 경로, 데이터 표현 같은 판단 단위 중심으로 다듬는다.
- `pro-designer` scenario testing baseline 문서를 제거해 skill spec을 기준 계약 중심으로 유지한다.
- `pro-planner` skill을 추가해 제품, 서비스, 기능 정의에서 사용자 문제, 가치, 범위, 요구사항, 우선순위, acceptance criteria, tradeoff, handoff 기준을 제공한다.
- `optimize-token` skill을 추가해 응답을 간결하게 압축하되 정확성, 검증 상태, 승인 경계, 필수 출력 형식을 보존하는 기준을 제공한다.
- `optimize-token` skill spec을 folder-based 구조로 전환하고, 용도별 surface intent scenario를 추가한다.
- `optimize-token` runtime에 용도별 reference를 추가해 응답, 진행 보고, 질문, 검증 보고, 승인 경계 문장을 간결하게 유지한다.
- `optimize-token` 압축 단계명을 `light`, `standard`, `extreme`으로 정렬하고, response intent scenario에 요청별 `Before`/`light`/`standard`/`extreme` 비교 예시를 추가한다.
- `optimize-token`의 `light`, `standard`, `extreme`은 response 전용이 아니라 skill 호출 전체의 강도이며, 모든 용도별 surface에 적용된다는 계약을 명시한다.
- `optimize-token` spec에 `levels/` child specs를 추가해 `light`, `standard`, `extreme` 단계별 entry, preservation, escalation, step-down gate를 명시한다.
- `optimize-token`에서 단계나 흐름 순서 설명은 `standard` 단계 규칙으로 두고 `>` 체인을 사용하도록 한다.
- `optimize-token` 단계 규칙을 `` `light` > `standard` > `extreme` `` 순서의 증분/override 구조로 정리해 spec 중복을 줄인다.
- `optimize-token` folder-based `spec.md`는 index와 경계만 남기고 상세 규칙을 child spec으로 위임한다.
- `optimize-token` child spec도 중복 설명을 줄이고, intent scenario는 표 형식으로 압축한다.
- `optimize-token` `levels/*.md`도 적용 조건과 덮어쓰기 기준 중심으로 더 압축한다.
- `optimize-token` `levels/*.md`에 단계별 짧은 예시를 포함한다.
- `optimize-token`의 response/thinking 및 용도별 surface 축을 제거하고, `levels/`는 전반 문구에 적용하며 저장되는 작성물만 `writing` 축으로 별도 관리한다.
- `optimize-token` 적용 범위를 user-facing 문구로 제한하지 않고, 코드/API/명령어 임의 축약 제외 범위 밖의 작성 문구 전반으로 확장한다.
- `levels/`는 `writing`을 제외한 나머지 모든 상황에 기본 적용되고, `writing`은 별도 관리한다.
- `optimize-token`의 중심 설명을 작성물 최적화가 아니라 에이전트 토큰 최적화로 정렬한다.
- 사용하지 않는 `tool-use-guide` skill과 spec을 제거하고 plugin usage surface에서 관련 노출을 정리한다.
- `git-committer` skill spec을 folder-based 구조로 전환하고, `intent.md`에 readiness-to-commit 책임 그래프를 둔다.
- `git-committer` runtime에 커밋 준비, 실행 권한, staged 검증, verification skip/failure 보고, post-commit 확인 경계를 명시한다.

## 사용자 스펙 의도

- 이 저장소의 플러그인들이 더 이상 disallowed temporary folder를 사용하거나 권장하지 않게 하고 싶다.
- `.agents/sessions` 폴더의 기본 규격을 `turn-gate`의 날짜 기반 `000-plan.md`와 `001-*` flow record 골격에 맞추고 싶다.
- 별도 session record schema와 controller를 제공하는 `session-manager`는 기본 session flow와 중복되므로 제거하고 싶다.
- `advance-codex`의 session folder guidance는 `turn-gate`를 대체하지 않고, 폴더 의미와 경계만 설명해야 한다.
- manifest prompt, README, plugin spec에서 plugin 사용 기준을 분명히 하고 싶다.
- passive skill의 `description`은 실제 사용 여부를 결정하는 트리거 조건이므로, 본문을 읽기 전 token matching에 걸리도록 설명 끝에 `#` 없는 쉼표 구분 plain token 목록을 둘 수 있어야 한다.
- subagent skill 표면은 실험 중 복잡도가 커졌으므로 `advance-codex`에서 제거하고 유지하지 않는다.
- 코드 작성과 문제 해결 과정에서 단순한 스타일 조언이 아니라 실제 증거, 원인 후보, 작은 완전 수정, 검증 기준을 중심으로 엔지니어링 판단을 적용하고 싶다.
- `pro-engineering` 스펙은 폴더화해 문제 해결, 엔지니어링 판단, 코드 작성 규율, 검증/보고의 상세 설명을 각각 분리하고 싶다.
- 자료조사, 구현 범위 조정, 문제 공간 파악처럼 직접 코딩 전 단계에서는 넓은 범위를 탐색하고, 실제 구현 단계에서는 코드를 좁은 범위로 집중해서 봐야 한다.
- 엔지니어링 판단에는 가까운 파일에 고치는 기준뿐 아니라 책임 소유자와 계층 경계를 먼저 정하는 소유권 경계 기준이 필요하다.
- blocklist 대응보다 allowlist 대응을 선호하는 기준을 기존 판단 축과 어울리게 다듬고 싶다.
- 조건문과 관련해 early-return 패턴 선호도 추가하고 싶다.
- 실제 작업 흐름에서 `tool-use-guide`가 선택되지 않았고, `advance-codex`의 핵심 사용 표면으로 유지할 필요가 낮으므로 제거하고 싶다.
- 실제 작업 흐름에서 `agents-sessions`도 거의 선택되지 않았고 `turn-gate`와 역할이 겹쳐 보이므로, runtime skill은 제거하고 session folder convention은 문서로만 남기고 싶다.
- 답변을 짧고 선명하게 다듬는 기준은 특정 외부 플러그인명이나 문체 명칭 없이 `advance-codex`의 `optimize-token` skill로 제공하고 싶다.
- `optimize-token`은 한국어 존대체만 특수하게 고정하기보다, 활성 언어와 말투를 보존하고 이 저장소처럼 한국어 존대체가 요구되는 환경에서는 그 기준을 따르게 하고 싶다.
- `optimize-token`은 단순히 "압축"이라고만 하지 않고, 압축 강도와 문법 보존 기준을 둬서 어색한 문장이 나오지 않게 하고 싶다.
- 20개 시나리오 평가 결과, 일반적인 "짧게" 요청이 과도하게 `dense`로 해석될 수 있어 `light` 기본값과 최강 압축 조건을 더 상세히 잠그고 싶다.
- 답변이나 작업을 하기 앞서 생각하는 단계에서도 간결하게 생각하는 방식을 다루고 싶다.
- `optimize-token`은 스킬 호출을 기반으로 `light`, `standard`, `extreme` 중 하나를 선택해 사용하는 방식이고, 이 모드는 응답, 진행 보고, 질문, 검증 보고, 승인 경계에 적용된다.
- `optimize-token`의 각 압축 단계는 단순한 스타일 이름이 아니라 통과해야 하는 gate를 가져야 하며, `extreme`도 안전, 승인, 검증, clarity gate를 통과하지 못하면 필요한 부분을 더 약한 단계로 강등해야 한다.
- 단계나 흐름을 설명할 때는 `standard` 단계에서 `>`를 사용해 읽는 순서를 보여주고 싶다. 예: `` `light` > `standard` > `extreme` 순으로 읽어야합니다. ``
- `light`, `standard`, `extreme` 관련 규칙은 증분되고 덮어씌워지도록 해서 스펙 문서를 간결하게 만들고 싶다.
- `spec.md`도 줄일 수 있으면 줄이고 싶다.
- 다른 optimize-token spec 파일들도 함께 줄이고 싶다.
- `levels/`도 spec 파일이므로 함께 줄이고 싶다.
- `levels/` 안에도 몇 가지 예시를 넣고 싶다.
- `optimize-token` 스킬 스펙에서 기존 축을 뒤엎고, `levels/light`, `levels/standard`, `levels/extreme`은 전반 적용하되 저장해야 되는 작성물은 `writing` 축으로 별도 관리하고 싶다.
- `optimize-token`은 user-facing이 아닌 부분까지도 전부 적용되어야 한다.
- `levels`는 `writing`을 제외한 나머지 모든 상황에서 기본 적용된다.
- `optimize-token`은 작성에 대한 내용으로 초점이 맞춰지면 안 되고, 에이전트 토큰 최적화가 주된 내용이어야 한다.
- `git-committer` 스펙은 폴더화하고, `intent.md`에 readiness 판단에서 실제 commit 실행까지의 책임 그래프를 보고 싶다.

## 변경 목적

`v0.1.1`은 session artifact 기능을 새 controller 중심으로 확장하는 release가 아니라, 기존 guidance를 `turn-gate` 중심의 session folder convention으로 정리하는 patch입니다.
기본 골격은 `.agents/sessions/{YYYYMMDD}/000-plan.md`와 `.agents/sessions/{YYYYMMDD}/{count-pad3}-{eng-lower-slug}.md`입니다.
`advance-codex`는 이 폴더의 목적과 경계를 설명하고, active turn continuity와 flow record 운영은 `loop-kit:turn-gate`가 소유합니다.

## 기록 근거

- 관련 커밋: `1117d40 feat: align agent session folders with turn-gate`
- 관련 세션 기록: `.agents/sessions/20260427/001-list-tmp-usage.md`
- 후속 formalization 기록: `.agents/sessions/20260427/008-formalize-advance-codex-change-specs.md`

## 변경 상세

### Temporary Folder Guidance 제거

- `workflow-kit-dev`와 release `workflow-kit`의 `autopilot` state guidance를 date-scoped `.agents/sessions/{YYYYMMDD}/` 경로로 바꾼다.
- `advance-codex-dev`와 release `advance-codex`에서 legacy scratch-folder guidance를 제거한다.
- 검증 기준은 runtime plugin surfaces에서 disallowed temporary-folder guidance가 남지 않는 것이다.

### `agents-sessions` 문서 축소

- `agents-sessions` runtime skill, agent metadata, skill spec을 제거한다.
- manifest prompt와 plugin spec의 callable skill 목록에서 `agents-sessions`를 제거한다.
- plugin spec과 README에는 `.agents/sessions/{YYYYMMDD}`가 session-scoped operational artifact 위치라는 문서 수준의 참고만 남긴다.

### `session-manager` 제거

- `session-manager` skill, spec, controller, tests, agent metadata, record format reference를 제거한다.
- `session-manager`의 legacy scratch-folder guidance를 제거한다.

### Cross-Plugin Alignment

- `workflow-kit-dev`와 release `workflow-kit`의 `autopilot` skill/spec이 `.agents/sessions/{YYYYMMDD}/` convention을 사용하도록 맞춘다.
- 이 동반 변경은 `advance-codex`의 session folder convention이 `workflow-kit` guidance와 충돌하지 않게 하기 위한 범위다.

### Subagent Runtime Skill 제거

- `subagent-gate`, `subagent-creator`, `subagent-work` runtime skill과 skill spec을 제거한다.
- manifest prompt, README, plugin spec에서 subagent orchestration skill 노출을 제거한다.
- `skill-scenario-testing`의 fresh subagent execution 언급은 평가 방식이므로 유지한다.

### `skill-scenario-testing` 운영 보강

- `skill-scenario-testing`은 target instruction을 직접 수정하거나 patch를 적용하지 않고, fresh executor evidence 기반 분석과 보고를 소유한다.
- 기본 empirical evaluation은 8개 scenario set을 기준으로 한다.
- smoke check는 2-3개 scenario로 축소할 수 있고, 사용자가 더 큰 scenario 수를 요청하면 N개 단위 batch로 나눠 dispatch한다.
- subagent 한도에 걸리면 완료됐거나 더 이상 쓰지 않는 executor를 닫고 다음 batch를 이어간다.

### Plugin-Level Routing 정리

- plugin usage guidance는 manifest prompt, README, plugin spec에 둔다.
- creator skill의 plugin packaging guidance는 manifest prompt, README, plugin spec 기준으로 정리한다.

### `tool-use-guide` 제거

- `tool-use-guide` runtime skill과 skill spec을 제거한다.
- manifest prompt, README, plugin spec에서 `tool-use-guide` 직접 노출을 제거한다.
- `skill-creator`에는 tool policy가 skill boundary를 흐리지 않게 하라는 일반 기준만 남기고, 제거된 sibling skill로 라우팅하지 않는다.

### `skill-creator` Description Trigger Metadata

- `skill-creator`는 skill frontmatter `description`을 body를 읽기 전 trigger metadata로 다루도록 안내한다.
- passive skill은 사용자가 명시적으로 skill을 요청하지 않아도 선택되어야 하므로, `description` 끝에 token matching을 돕는 plain token 목록을 쉼표로 나열한다.
- token 목록은 literal hashtag가 아니므로 `#`를 붙이지 않는다.
- active/direct-call skill이나 이미 충분히 좁은 trigger를 가진 skill에는 불필요한 token 목록을 강제하지 않는다.

### `pro-engineering` 추가

- `pro-engineering` skill을 추가해 코드 작성, 버그 수정, 리팩터링, 설계 판단에서 문제 해결 중심의 engineering judgment를 제공한다.
- skill의 핵심 루프는 증상과 기대 동작 분리, 실제 증거 수집, 원인 후보 나열, 가설 검증, 작은 완전 수정, 검증 가능한 결과, residual risk 보고다.
- 이 skill은 특정 언어/프레임워크 레시피, 도구 사용 정책, 커밋 절차, subagent lifecycle을 소유하지 않는다.
- passive skill로 선택될 수 있도록 runtime `description` 끝에 `#` 없는 쉼표 구분 plain token 목록을 둔다.
- spec은 `specs/skills/pro-engineering/intent.md`, `spec.md`, `core/*.md`로 나누어 사용자 의도, index, 세부 계약을 분리한다.
- `core/problem-solving.md`는 증상과 기대 동작 분리, 증거 수집, 원인 후보, 가설 검증, 수정 진입 기준을 소유한다.
- `core/scope-control.md`는 조사/범위 설정 단계의 넓은 탐색과 구현 단계의 좁은 집중 사이 전환 기준을 소유한다.
- `core/engineering-judgment.md`는 기존 패턴 우선, 소유권 경계, 추상화 판단, 명시적 계약, 리스크 배수, 사용자 입력 필요 조건을 소유한다.
- `core/engineering-judgment.md`는 금지 목록 확장보다 허용되는 입력, 상태, 효과, 실패 조건을 먼저 정의하는 허용 조건 우선 계약을 소유한다.
- `core/code-discipline.md`는 작은 완전 변경, 단순 구현 후 개선, 변경 범위, 제어 흐름 명확성, 코드 품질, 금지되는 작성 방식을 소유한다.
- `core/verification-reporting.md`는 검증 선택, 실패 처리, 완료 판정, 보고 형식을 소유한다.

### `pro-designer` 추가

- `pro-designer` skill을 추가해 화면, 제품 UI, 시각 산출물 작업에서 디자인 지식 기준을 적용하게 한다.
- 이 skill은 디자인 지식과 판단 기준을 중심으로 둔다.
- 핵심 판단 축은 UI, UX, Content, Quality core 기준과 별도 Color, Tone & Expression, Branding, Space & Composition, Surface & Form 축이다.
- Color 축은 색상 역할, 의미 색상, 파스텔톤 같은 색감, 테마 적응성, 데이터 색상, 문화와 도메인 맥락을 소유한다.
- Tone & Expression 축은 목표 톤 분류, 톤 적합성, 톤 구성 요소, 톤 경계를 소유한다.
- Branding 축은 브랜드 식별성, 브랜드 자산, 브랜드 색상 역할, 브랜드 일관성, 기능 의미와의 충돌 방지를 소유한다.
- Space & Composition 축은 공간 모델, 영역 구조, 밀도와 리듬, 정보 밀도 적합성, 공간 위계, 흐름과 길찾기, 관계 기하, 반응형 재구성을 소유한다.
- Surface & Form 축은 카드, 패널, 버튼, 입력 영역의 표면 모델, 깊이, 형태 언어, 물성, 촉각성을 소유한다.
- UI, UX, Color, Surface & Form, Quality 기준은 사례별 방지 규칙이 아니라 크기, 간격, 색, 선, 표면, 메시지 단위, 구분 방식 같은 판단 단위를 중심으로 설명한다.
- UI 기준은 폰트 크기, placeholder, 버튼, 아이콘 버튼, 관계도, 차트, 표 같은 표면 요소를 별도 사례 기준으로 늘리지 않고 정보 구조, 시각 위계, 컴포넌트 역할, affordance, 데이터 표현으로 판단한다.
- UX 기준은 화면 안의 다음 경로와 상세 진입 가능성을 탐색성 기준으로 판단한다.
- Color 기준은 데이터 값, 축, 격자, 라벨, series, threshold, 범례를 장식보다 의미 구분 기준으로 판단한다.
- Surface & Form 기준은 카드, 패널, band, plain section, list row, table row가 비교, 묶음, 독립 행동, 강조, 배경 분리 중 어떤 역할을 맡는지 판단한다.
- Space & Composition 기준은 Layout의 기능 배치와 분리해 화면이 문서, 콘솔, 캔버스, 지도, 토폴로지, 그리드, split view 같은 공간 모델로 이해되는지 판단한다.
- Space & Composition 기준은 고밀도 화면의 한눈에 비교 가능한 정보량과 저밀도 화면의 실수 방지·이해 중심 정보량을 사용 목적에 맞게 판단한다.
- 목표 톤은 사례별 기준을 늘리지 않고 tone type, fit, construction, boundaries로 판단한다.
- 브랜드 표현은 tone과 분리해 특정 브랜드로 인식되는지, 그리고 CTA, 상태색, 위험색, 제품 이해를 침범하지 않는지 판단한다.
- spec은 `specs/skills/pro-designer/intent.md`, `spec.md`, `core/*.md`로 나누어 사용자 의도, index, 세부 계약을 분리한다.
- runtime `skills/pro-designer/SKILL.md`는 설치 후 dev-only spec 경로 없이 독립적으로 디자인 판단 기준을 제공한다.
- 별도 `scenario-testing.md` baseline은 두지 않는다.

### `pro-planner` 추가

- `pro-planner` skill을 추가해 제품, 서비스, 기능 요청에서 product planning judgment를 제공한다.
- 핵심 판단 축은 Problem, User, Value, Scope, Requirements, Flow, Acceptance, Priority, Tradeoff, Handoff다.
- 이 skill은 화면 배치, 시각 표현, 컴포넌트 디자인, 코드 구현, 기술 설계, 테스트 전략을 소유하지 않는다.
- passive skill로 선택될 수 있도록 runtime `description` 끝에 `#` 없는 쉼표 구분 plain token 목록을 둔다.
- spec은 `specs/skills/pro-planner/intent.md`, `spec.md`, `core/*.md`로 나누어 사용자 의도, index, 세부 계약을 분리한다.
- `core/problem-value.md`는 사용자 문제, 대상 사용자, 사용 맥락, 가치, use case 판단을 소유한다.
- `core/scope-requirements.md`는 MVP, non-goal, 요구사항, acceptance criteria를 소유한다.
- `core/priority-tradeoff-handoff.md`는 우선순위, product tradeoff, designer/engineer handoff 계약을 소유한다.
- runtime `skills/pro-planner/SKILL.md`는 설치 후 dev-only spec 경로 없이 독립적으로 기획 판단 기준을 제공한다.

### `optimize-token` 추가

- `optimize-token` skill을 추가해 응답 표면의 토큰 낭비를 줄이는 기준을 제공한다.
- 이 skill은 context/session compaction, prompt rewriting, 코드 축약, 안전 고지 축소를 소유하지 않는다.
- 저장되는 작성물 기준은 runtime reference인 `skills/optimize-token/references/writing.md`가 소유한다.
- `light`, `standard`, `extreme`은 skill 호출 전체의 압축 강도이며, 용도별 surface 전체에 적용된다.
- 응답 압축 중에도 활성 언어, 말투, 저장소별 출력 기대사항을 유지한다.
- 기본 압축 강도는 `light`로 두고, 일반적인 "짧게/간결하게" 요청은 먼저 `light`로 처리한다. 필요할 때 `standard`, `extreme`으로 올리되 `extreme`은 라벨/상태/한 줄/최대 압축처럼 출력 형태가 분명할 때만 사용한다.
- runtime text에는 특정 외부 플러그인명, 캐릭터 프레이밍, novelty style label을 남기지 않는다.
- manifest prompt, README, plugin spec은 `optimize-token`의 역할을 언급한다.
- `optimize-token` skill spec은 folder-based 구조를 사용하고, `intent-scenarios/writing.md`는 writing 축의 `Before`/`light`/`standard`/`extreme` 비교 예시를 소유한다.
- `optimize-token` skill spec의 `levels/` 폴더는 각 압축 단계가 통과해야 하는 entry gate, preservation gate, escalation 또는 step-down gate를 소유한다.
- `optimize-token`의 단계 규칙은 뒤 단계가 앞 단계 규칙을 상속하고 자기 override만 적는 구조로 유지한다.
- `optimize-token`의 folder-based `spec.md`는 상세 규칙을 반복하지 않고 목적, 경계, 표면 map, 핵심 위임 계약, 확장 원칙만 유지한다.
- `optimize-token`의 `response.md`와 intent scenario는 반복 설명보다 압축된 규칙과 표 기반 예시를 우선한다.
- `optimize-token`의 `levels/*.md`는 긴 설명 대신 적용 조건과 덮어쓰기 기준만 남긴다.
- `optimize-token`의 `levels/*.md` 예시는 단계별 판단 차이를 보여주되 짧게 유지한다.
- `optimize-token`은 단계나 흐름의 순서 설명을 `standard` 단계 규칙으로 다루며, `` `light` > `standard` > `extreme` ``처럼 `>` 체인을 사용한다.
- `optimize-token`은 별도 response/thinking/surface 축을 소유하지 않고, `levels/`를 에이전트 출력과 작업 운영 문구 전반에 적용한다.
- `optimize-token`은 저장되는 작성물만 `writing` 축으로 관리하며, runtime은 `references/writing.md`를 해당 경우에만 읽는다.
- `optimize-token` `extreme`은 필드값, 표 셀, 리스트 속성값의 결측값을 `-`로 줄일 수 있으며, 문장 중간에는 `-`를 쓰지 않는다.
- `optimize-token` `standard`는 관련 list 항목을 상위 항목과 sublist로 group화해 반복 표현을 줄일 수 있다.
- `optimize-token` `extreme`은 `standard`의 list group을 상속하되, 필드형이나 한 줄 표기가 더 짧으면 더 짧은 쪽을 쓴다.
- runtime level 요약은 `skills/optimize-token/references/levels.md`가 소유하고, `SKILL.md`는 reference 라우팅과 핵심 workflow만 유지한다.

### `git-committer` Folder-Based Spec 전환

- `git-committer` flat skill spec을 `specs/skills/git-committer/` 폴더 구조로 전환한다.
- `intent.md`는 사용자 의도와 readiness-to-commit 책임 그래프를 소유한다.
- `spec.md`는 목적, 경계, 대표 표면, sub-spec map, 핵심 처리 계약, 검증 기준을 소유한다.
- `workflow.md`, `message.md`, `verification.md`는 commit approval, lifecycle, message, staged scope, skip/failure, post-commit 확인 계약을 나눠 소유한다.
- runtime `SKILL.md`는 현재 folderized spec 기준으로 재작성하고 dev-only spec 경로를 실행 지시로 남기지 않는다.
- `git-committer`는 커밋 준비, 커밋 실행 권한, 커밋 실행 흐름을 분리하고 readiness 통과, 검증 통과, handoff, session record만으로 commit 실행 승인을 추정하지 않는다.

## 비목표

- `turn-gate` flow record 생성/갱신 규칙을 `advance-codex` 문서나 다른 sibling skill에 복제하거나 재정의하지 않는다.
- 제거된 `tool-use-guide` 책임을 다른 sibling skill에 그대로 흡수하지 않는다.
- 제거된 subagent runtime skill 책임을 다른 sibling skill에 그대로 흡수하지 않는다.
- `pro-engineering`을 특정 언어/프레임워크 구현 레시피나 프로젝트별 코딩 컨벤션 대체물로 만들지 않는다.
- `optimize-token`을 일반 context compression, session record 요약, prompt rewriting, 코드 minification 도구로 만들지 않는다.
- `optimize-token` progress surface를 내부 추론 공개나 장황한 방법 설명으로 만들지 않는다.
- 별도 `session_record.md`, `change_record.md`, `retrospective_record.md` schema를 유지하지 않는다.
- `.agents/sessions`를 plugin source, release surface, reusable spec의 대체 위치로 만들지 않는다.
- `workflow-kit`의 autopilot loop 자체를 재설계하지 않는다.
- 독립 실행 표면의 이름이나 책임을 바꾸지 않는다.

## 호환성 및 마이그레이션

- `session-manager`를 사용하던 호출자는 더 이상 이 plugin에서 해당 skill이나 controller를 찾을 수 없다.
- active turn continuity와 flow 기록은 `loop-kit:turn-gate`를 사용한다.
- `.agents/sessions`의 폴더 의미는 `advance-codex` README와 plugin spec의 문서 참고를 따른다.
- autopilot state/resume artifacts는 date-scoped `.agents/sessions/{YYYYMMDD}/` 아래에 둔다.
- 사용자는 필요한 구체 skill을 직접 호출한다.

## 검증 기준

- runtime skill source와 release surface 모두에서 `session-manager` runtime surface가 없어야 한다.
- runtime skill source와 release surface 모두에서 disallowed temporary-folder guidance가 없어야 한다.
- `advance-codex` dev/release surface에 `agents-sessions` runtime skill, skill spec, agent metadata가 없어야 한다.
- `workflow-kit` dev/release `autopilot` guidance가 `.agents/sessions/{YYYYMMDD}/`를 사용해야 한다.
- release surface에는 `specs/` 디렉터리가 포함되지 않아야 한다.
- dev/release surface는 manifest prompt에 적힌 구체 skill만 제공해야 한다.
- dev/release surface에는 `subagent-gate` runtime skill과 skill spec이 없어야 한다.
- dev/release surface에는 `subagent-creator` runtime skill과 skill spec이 없어야 한다.
- dev/release surface에는 `subagent-work` runtime skill과 skill spec이 없어야 한다.
- dev/release `skill-scenario-testing` skill은 분석 중심 evaluation boundary, 기본 8개 scenario set, batch dispatch, subagent limit handling을 설명해야 한다.
- dev/release `skill-creator` skill은 passive skill `description` 끝 plain token 목록 규칙을 설명해야 하며, 이 목록이 literal `#` hashtag가 아님을 명시해야 한다.
- dev/release `pro-engineering` runtime skill이 존재해야 하며, 문제 해결 중심의 engineering judgment, root cause analysis, implementation discipline, verification, residual risk reporting을 설명해야 한다.
- dev/release manifest prompt, README, plugin spec은 `pro-engineering`의 역할을 언급해야 한다.
- dev/release `pro-designer` runtime skill이 존재해야 하며 UI, UX, Content, Color, Tone & Expression, Branding, Space & Composition, Surface & Form, Quality의 디자인 판단 기준을 설명해야 한다.
- dev/release manifest prompt, README, plugin spec은 `pro-designer`의 역할을 언급해야 한다.
- dev/release `pro-planner` runtime skill이 존재해야 하며 Problem, User, Value, Scope, Requirements, Flow, Acceptance, Priority, Tradeoff, Handoff의 제품 기획 판단 기준을 설명해야 한다.
- dev/release manifest prompt, README, plugin spec은 `pro-planner`의 역할을 언급해야 한다.
- `src/advance-codex-dev/specs/skills/pro-planner.md` flat spec은 없어야 하며, folder-based `intent.md`, `spec.md`, `core/*.md`가 존재해야 한다.
- dev/release `pro-planner` runtime은 dev-only spec 경로를 실행 지시로 남기지 않아야 한다.
- dev/release `pro-planner` runtime은 UI 표현 기준이나 코드 구현 기준을 소유하지 않아야 한다.
- dev/release `optimize-token` runtime skill과 `references/writing.md`가 존재해야 한다.
- dev/release manifest prompt, README, plugin spec은 `optimize-token`의 역할을 언급해야 한다.
- dev/release `optimize-token` surface에는 특정 외부 플러그인명이나 캐릭터화된 style label이 없어야 한다.
- dev/release `optimize-token` surface는 응답 압축 중에도 활성 언어와 말투를 유지해야 한다.
- dev/release `optimize-token` surface는 압축 강도 선택 기준과 문법 보존 기준을 설명해야 한다.
- dev/release `optimize-token`은 전반 문구에 level gate를 적용하되 내부 추론 공개로 흐르지 않게 해야 한다.
- dev/release `optimize-token` writing reference는 저장되는 작성물의 현재 상태 기준, 검증 상태, 승인 경계 보존 기준을 설명해야 한다.
- dev/release `optimize-token` surface는 단계나 흐름 순서 설명을 `standard` 단계 규칙으로 두고 `>` 체인 표기 기준을 설명해야 한다.
- `src/advance-codex-dev/specs/skills/optimize-token.md` flat spec은 없어야 하며, folder-based `intent.md`, `spec.md`, `writing.md`, `intent-scenarios/writing.md`가 존재해야 한다.
- `src/advance-codex-dev/specs/skills/optimize-token/levels/light.md`, `standard.md`, `extreme.md`가 존재하고 각 단계의 gate를 설명해야 한다.
- `levels/standard.md`와 `levels/extreme.md`는 앞 단계 규칙을 반복하지 않고 override 중심으로 설명해야 한다.
- `src/advance-codex-dev/specs/skills/optimize-token/spec.md`는 folder-based index로 간결해야 하며 상세 gate와 예시는 child spec이 소유해야 한다.
- `src/advance-codex-dev/specs/skills/optimize-token/intent-scenarios/*.md`는 예시 검증 가치를 유지하되 반복되는 설명 문장을 표로 압축해야 한다.
- dev/release `optimize-token` surface는 최강 압축 단계명을 `extreme`으로 사용하고, stale `dense` 단계명을 남기지 않아야 한다.
- dev/release surface에는 `tool-use-guide` runtime skill과 skill spec이 없어야 한다.
- dev/release manifest prompt, README, plugin spec, routing 문구에는 제거된 `tool-use-guide` 사용 안내가 없어야 한다.
- `src/advance-codex-dev/specs/skills/git-committer.md` flat spec은 없어야 하며, folder-based `intent.md`, `spec.md`, `workflow.md`, `message.md`, `verification.md`가 존재해야 한다.
- dev/release `git-committer` runtime은 커밋 준비, 실행 권한, staged 검증, commit 실행 경계를 명시해야 한다.
- dev/release `git-committer` runtime은 verification skip/failure를 pass로 숨기지 않아야 한다.
- dev/release manifest prompt, README, plugin spec은 `git-committer`의 커밋 준비, 실행 권한, staged 검증, commit 실행 역할을 언급해야 한다.
- `src/advance-codex-dev/specs/skills/pro-engineering.md` flat spec은 없어야 하며, folder-based `intent.md`, `spec.md`, `core/*.md`가 존재해야 한다.
- dev/release `pro-engineering` runtime은 직접 코딩 전 넓은 탐색과 구현 단계의 좁은 집중 기준을 설명해야 한다.
- dev/release `pro-engineering` runtime은 blocklist 조건 추가보다 허용 조건과 명시적 실패 조건 정의를 우선하고, 차단 조건은 좁은 보조 방어선으로 다뤄야 한다고 설명해야 한다.
- dev/release `pro-engineering` runtime은 guard, 실패 조건, 처리 불가 상태를 early return으로 분리하는 기준과 단일 exit이 더 안전한 예외 범위를 설명해야 한다.

## 정식 규칙 승격 여부

- 정식 규칙으로 승격할 항목: change spec 경로와 요약-first 문서 형식은 repo-level `AGENTS.md`와 `docs/templates/change-spec.md`가 소유한다.
- plugin docs로 유지할 항목: `.agents/sessions/{YYYYMMDD}` folder-boundary convention은 README와 plugin spec에만 간결히 남긴다.
- change spec에만 남길 이력: `session-manager` 제거 배경, temporary-folder guidance 제거 과정, `workflow-kit` autopilot 동반 정렬의 release history.
- change spec에만 남길 이력: plugin 사용 기준 정리 범위.
