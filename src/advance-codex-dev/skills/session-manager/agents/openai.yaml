interface:
  display_name: "Session Manager"
  short_description: "Manage session records and change records"
  default_prompt: "Use $session-manager to manage session_record.md, change_record.md, or retrospective_record.md under .agents/sessions/session-id, using `.tmp` only for scratch artifacts."
