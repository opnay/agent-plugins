# workflow-kit-dev v0.1.1 Change Spec

## 변경사항 요약

- `autopilot` state guidance를 date-scoped `.agents/sessions/{YYYYMMDD}/` convention으로 정렬한다.
- `turn-gate`가 visible next-flow choices가 가득 찬 경우에도 turn-end option을 기록하도록 계약을 강화한다.
- `turn-gate-self-drive`를 별도 plugin skill overlay로 분리하고, base `turn-gate`는 conversation continuity를 소유하게 유지한다.
- `turn-gate`를 conversation-level first-class control rule로 명시하고, skill body를 spec-driven runtime surface로 재작성한다.

## 사용자 스펙 의도

- temporary or ad hoc session paths 대신 `.agents/sessions/{YYYYMMDD}/` convention을 사용해야 한다.
- `turn-gate`를 사용한 turn은 사용자가 명시적으로 끝내기 전까지 terminal summary로 닫히면 안 된다.
- autonomous question routing은 base `turn-gate` 안에 섞지 않고 `turn-gate-self-drive`가 소유해야 한다.
- `workflow-kit`은 `loop-kit`이 흡수해 쓰는 canonical upstream contract를 함께 고쳐야 한다.

## 변경 목적

`v0.1.1`은 `workflow-kit`의 canonical workflow contracts를 `turn-gate` 중심으로 정렬하는 patch입니다.
이 버전은 `autopilot` artifact location, base `turn-gate` continuity, self-drive overlay ownership, skill body composition을 같은 release line 안에서 정리합니다.
`loop-kit`은 이 canonical contract를 operational package로 흡수해 사용합니다.

## 기록 근거

- 관련 커밋: `1117d40 feat: align agent session folders with turn-gate`
- 관련 커밋: `740390d docs: require recorded turn-end option in turn-gate`
- 관련 커밋: `66b236f feat: split turn-gate self-drive overlay`
- 관련 커밋: `4812037 docs: clarify self-drive plugin skill ownership`
- 관련 커밋: `a76cbe2 docs: strengthen turn-gate conversation rule`
- 관련 커밋: `c4382c4 docs: make turn-gate skill body spec-driven`
- 관련 세션 기록: `.agents/sessions/20260427/001-list-tmp-usage.md`
- 관련 세션 기록: `.agents/sessions/20260427/004-assess-self-drive-wording.md`
- 관련 세션 기록: `.agents/sessions/20260427/005-update-workflow-kit-self-drive-wording.md`
- 관련 세션 기록: `.agents/sessions/20260427/006-strengthen-turn-gate-first-class-rule.md`
- 관련 세션 기록: `.agents/sessions/20260427/007-rewrite-turn-gate-dev-skills.md`

## 변경 상세

### Autopilot Session Path

- `autopilot` state and QA issue guidance는 `.agents/sessions/{YYYYMMDD}/` 아래를 사용한다.
- 이 변경은 `advance-codex`의 `agents-sessions` folder convention과 충돌하지 않게 하기 위한 cross-plugin alignment다.

### Recorded Turn-End Option

- `turn-gate`는 visible next-flow choices가 이미 가득 찬 경우에도 flow record의 `Next Flow Options`에 turn-end option을 기록한다.
- 사용자가 명시적으로 turn을 끝내기 전에는 result report가 terminal summary 역할을 하지 않는다.

### Self-Drive Overlay

- `turn-gate-self-drive` skill/spec을 추가해 autonomous subagent question routing을 별도 plugin skill로 분리한다.
- base `turn-gate` skill/spec/template에서는 self-drive packet, answer, pause, recovery 세부사항을 제거한다.
- self-drive overlay는 같은 plugin의 `turn-gate` contract를 base continuity로 사용한다.

### Conversation-Level First-Class Rule

- `turn-gate` skill/spec은 active turn에서 `turn-gate` flow가 conversation response 자체의 first-class control rule임을 명시한다.
- `user_explicit_stop`이 false인 동안 final-answer channel로 loop를 종료하지 않는 guardrail을 강화한다.

### Spec-Driven Skill Body

- `turn-gate` skill spec에 skill body composition contract를 추가한다.
- `turn-gate` skill body를 spec에서 요구하는 phase loop, guardrails, bad ending patterns, session record handling을 보존하는 runtime surface로 재작성한다.

## 비목표

- `loop-kit`의 narrow operational packaging을 `workflow-kit` 안으로 흡수하지 않는다.
- `turn-gate-self-drive`가 base `turn-gate` continuity를 대체하지 않는다.
- `autopilot`의 전체 loop semantics를 재설계하지 않는다.
- user-gated question routing을 제거하지 않는다.

## 호환성 및 마이그레이션

- `workflow-kit-dev` manifest는 `0.1.0`에서 `0.1.1`로 올라간다.
- `autopilot` artifacts는 date-scoped `.agents/sessions/{YYYYMMDD}/` 아래에 둔다.
- self-drive behavior를 원하는 호출자는 base `turn-gate`가 아니라 `turn-gate-self-drive`를 명시적으로 사용한다.
- `loop-kit`은 이 release line의 `turn-gate` and `turn-gate-self-drive` contract를 operational surface로 동기화한다.

## 검증 기준

- dev/release `workflow-kit` manifests가 valid JSON이고 `0.1.1` version을 유지해야 한다.
- release `workflow-kit` surface에는 `specs/` directory가 없어야 한다.
- dev/release `autopilot` guidance가 `.agents/sessions/{YYYYMMDD}/` convention을 사용해야 한다.
- dev/release `turn-gate` skill/spec/template에서 recorded turn-end option, conversation-level first-class rule, and terminal-summary guardrail이 확인되어야 한다.
- dev/release `turn-gate-self-drive` skill/spec이 separate plugin skill overlay ownership을 설명해야 한다.

## 정식 규칙 승격 여부

- plugin/skill contract로 승격할 항목: `turn-gate`, `turn-gate-self-drive`, and `autopilot` normative contracts are owned by their corresponding `specs/skills/*.md` files.
- repo-level rule로 승격할 항목: plugin-scoped skill 변경은 upstream canonical surface까지 함께 확인해야 한다는 원칙은 `AGENTS.md`가 소유한다.
- change spec에만 남길 이력: `0.1.1` 안에서 여러 follow-up commits가 같은 version surface를 갱신한 release history.
