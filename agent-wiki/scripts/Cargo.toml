[package]
name = "agent-wiki"
version = "0.1.0"
edition = "2024"
publish = false
autotests = false

[dependencies]
tempfile = "3.27"
toml = "1.1"

[profile.release]
strip = true
lto = true
codegen-units = 1
